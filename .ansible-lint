profile: production # min, basic, moderate,safety, shared, production

exclude_paths:
  - roles-external/
  - collections/
  - .github/

skip_list:
  - yaml[line-length]
  - role-name
  - name[play]
  - name[casing]
  - name[template]
  - var-naming[no-role-prefix]
